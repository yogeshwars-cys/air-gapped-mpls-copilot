global:
  scrape_interval: 1s         # High-resolution 1-second scraping for real-time ML input
  evaluation_interval: 1s

scrape_configs:
  - job_name: 'mpls_simulation_exporter'
    static_configs:
      - targets: ['host.docker.internal:8000'] # Points to the custom exporter running on the host
